// combined error + run autotest sample

//#define ERRORTEST

#ifdef ERRORTEST

var missing_type = 1;  //?error(TypeSpecExpected)
badstatement           //?error(ModStatementUnknown)

#else

[[external]] function printf(fmt : ^cchar, ...) -> int;

function main() -> int:
  printf("combined hello\n");   //?check('combined hello')
  printf("combined id=1\n");    //?check('combined id', 1)
  return 0;
endfunc

#endif


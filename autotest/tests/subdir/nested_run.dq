// nested runtime sample

[[external]] function printf(fmt : ^cchar, ...) -> int;

function main() -> int:
  printf("nested hello\n");   //?check(nested hello)
  printf("nested id=30\n");   //?check(nested id, 30)
  return 0;
endfunc


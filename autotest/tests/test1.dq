// dq auto-test file

//#define ERRORTEST

#ifdef ERRORTEST

var i : int = 5;  //?error(TypeUnknown)
var i = 1;        //?error(TypeSpecExpected)

asdf

#else

[[external]] function printf(fmt : ^cchar, ...) -> int;

function main() -> int:
  var strvar : cstring[64] = "abcd";
  printf("Hello\n");      //?check(Hello)
  printf("Hello2=5\n");   //?check(Hello2, 5)
  printf("strvar = \"%s\"\n", &strvar);  //?check(strvar,  "abcd")
                                         //?check(strvar2, "efg")
  return 0;
endfunc

#endif

/* dqatrun output, ERROR TEST

Error test: "test1.dq"
-------------------------------------------------------------------------------
- test1.dq(7) missing ERROR(TypeUnknown)
? test1.dq(10,1) ERROR(ModStatementUnknown): Unknown module statement "asdf"
-------------------------------------------------------------------------------
Error test FAILED: 2 failures detected.

(successful case)

Error test: "test1.dq"
Error test PASSED.

*/

/* dqatrun output, RUN TEST:

Run test: "test1.dq"
-------------------------------------------------------------------------------
Hello                                  `
Hello2=5                               ` != 6
Hello2=                                ` != 6
Hello2=1                               ` != 6
Hello2                                 ` != 6
strvar = "abcd"                        ` unchecked
                                       ` missing strvar2 = "efg"
-------------------------------------------------------------------------------
Run test FAILED: 6 failures detected.

(successful case)

Run test: "test1.dq"
-------------------------------------------------------------------------------
Hello                                  `
Hello2=5                               `
strvar = "abcd"                        `
-------------------------------------------------------------------------------
Run test PASSED.

*/